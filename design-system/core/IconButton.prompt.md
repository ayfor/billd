A square pixel button for a single icon — toolbars, close buttons, row actions.

```jsx
<IconButton icon="close" label="Close" />
<IconButton icon="settings" variant="ghost" />
```

Variants: `default` (outlined) | `ghost` (borderless). Always pass `label` for
accessibility when the icon alone isn't self-describing.
