The ornamental cross-stitch band — billd's one decorative flourish. Use it **once per view** (the guardrail: one motif maximum).

```jsx
<PixelDivider src="../assets/pixel-divider.svg" />
```

Point `src` at `assets/pixel-divider.svg` relative to the page.
