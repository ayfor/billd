A native select styled to match billd inputs, with a pixel chevron.

```jsx
<Select label="Category" options={['Groceries','Dining out','Transit','Hobbies','Rent']} />
<Select options={[{value:'jun',label:'June 2026'}]} />
```
