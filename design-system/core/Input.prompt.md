A crisp modern text field with an optional uppercase label, hint, and inline prefix. Inputs deliberately stay out of the pixel world — they're calm and legible.

```jsx
<Input label="Description" placeholder="What was it for?" />
<Input label="Amount" prefix="$" size="lg" inputMode="decimal" />
<Input label="Email" hint="We never share it." />
```
